{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-event-listener",
  "description": "Allow the main Visual Office window to listen for and clean up workspace change events emitted by the local Tauri backend.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
